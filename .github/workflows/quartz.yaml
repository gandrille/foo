# ======================================
name: Deploy Quartz site to GitHub Pages
# ======================================

on:
  # Runs on pushes targeting the default branch
  push:
    branches:
      - main

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment, skipping runs queued between the run in-progress and latest queued.
# However, do NOT cancel in-progress runs as we want to allow these production deployments to complete.
concurrency:
  group: "pages"
  cancel-in-progress: false

# Default to bash
defaults:
  run:
    shell: bash

jobs:
  # Build job
  build:
    runs-on: ubuntu-latest
    env:
      ZIM_VERSION: 0.75.2
      GENERATE_PDF: false

    steps:
      - uses: actions/setup-node@v4
        with:
          node-version: 20

      # Empty quartz setup
      # ==================
          
      - name: Checkout Quartz
        uses: actions/checkout@v4
        with:
          repository: 'jackyzha0/quartz'
          path: 'quartz'
      - run: cd quartz && npm i
      - run: cd quartz && npx quartz create --strategy new --links shortest
      
      # Quartz customization with content and config
      # ============================================

      - name: Checkout Source
        uses: actions/checkout@v4
        with:
          path: 'content'

      - name: List all
        run: find .





# git clone https://github.com/jackyzha0/quartz.git
# cd quartz
# npm i
# npx quartz create --strategy new --links shortest
# cp /home/etienne/documents/linux-notes/* content/
# mv content/quartz.*.ts .
# npx quartz build


